import { WebContents } from 'electron'

// Stub implementation — Plan 02 implements the full PTY lifecycle
export function registerPtyHandlers(_webContents: WebContents): void {
  // No-op until Plan 02
}
