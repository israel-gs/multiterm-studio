---
phase: 02-multi-panel-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/renderer/src/store/panelStore.ts
  - src/renderer/src/components/MosaicLayout.tsx
  - src/renderer/src/components/PanelWindow.tsx
  - src/renderer/src/App.tsx
  - src/renderer/src/components/Terminal.tsx
  - src/renderer/src/assets/global.css
  - tests/store/panelStore.test.ts
  - tests/renderer/MosaicLayout.test.tsx
autonomous: true
requirements: [LAYOUT-01, LAYOUT-03, LAYOUT-05, LAYOUT-08]

must_haves:
  truths:
    - "App renders a tiling mosaic layout with at least one terminal panel on startup"
    - "User can click '+ New terminal' to add a panel; both panels have live PTY sessions"
    - "User can drag dividers to resize panels and terminal text reflows correctly"
    - "Closing the last panel shows a zero-state view with a '+ New terminal' prompt"
    - "When a panel is removed from the tree, its PTY process is killed via IPC"
  artifacts:
    - path: "src/renderer/src/store/panelStore.ts"
      provides: "Zustand store for panel metadata (title, color per sessionId)"
      exports: ["usePanelStore"]
    - path: "src/renderer/src/components/MosaicLayout.tsx"
      provides: "Controlled Mosaic<string> wrapper with onChange diff, addPanel, zero-state"
      exports: ["MosaicLayout"]
    - path: "src/renderer/src/components/PanelWindow.tsx"
      provides: "MosaicWindow wrapper with renderToolbar and TerminalPanel child"
      exports: ["PanelWindow"]
    - path: "tests/store/panelStore.test.ts"
      provides: "Unit tests for panelStore actions"
    - path: "tests/renderer/MosaicLayout.test.tsx"
      provides: "Unit tests for mosaic layout behavior"
  key_links:
    - from: "src/renderer/src/App.tsx"
      to: "src/renderer/src/components/MosaicLayout.tsx"
      via: "renders MosaicLayout as root layout"
      pattern: "<MosaicLayout"
    - from: "src/renderer/src/components/MosaicLayout.tsx"
      to: "src/renderer/src/components/PanelWindow.tsx"
      via: "renderTile callback"
      pattern: "renderTile.*PanelWindow"
    - from: "src/renderer/src/components/PanelWindow.tsx"
      to: "src/renderer/src/components/Terminal.tsx"
      via: "renders TerminalPanel as child of MosaicWindow"
      pattern: "<TerminalPanel"
    - from: "src/renderer/src/components/MosaicLayout.tsx"
      to: "window.electronAPI.ptyKill"
      via: "onChange diff detects removed leaves"
      pattern: "ptyKill"
    - from: "src/renderer/src/components/MosaicLayout.tsx"
      to: "src/renderer/src/store/panelStore.ts"
      via: "addPanel/removePanel on tree mutations"
      pattern: "usePanelStore"
---

<objective>
Install react-mosaic-component and zustand, create the controlled mosaic layout with a zustand panel store, wire App.tsx to render the tiling canvas, and implement PTY lifecycle management (kill on panel close). This delivers a working multi-panel terminal workspace where users can add panels, resize via drag dividers, and close panels cleanly.

Purpose: Transform the single-terminal app into a multi-panel tiling workspace -- the core structural change of Phase 2.
Output: MosaicLayout.tsx, PanelWindow.tsx, panelStore.ts, updated App.tsx, updated Terminal.tsx, tests for store and layout.
</objective>

<execution_context>
@/Users/israelgs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/israelgs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-multi-panel-layout/02-RESEARCH.md
@.planning/phases/02-multi-panel-layout/02-VALIDATION.md
@.planning/phases/01-foundation-terminal-core/01-03-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From src/renderer/src/components/Terminal.tsx:
```typescript
interface Props {
  sessionId: string
  cwd: string
}
export function TerminalPanel({ sessionId, cwd }: Props): React.JSX.Element
```

From src/preload/index.d.ts (also src/renderer/src/env.d.ts):
```typescript
interface Window {
  electronAPI: {
    ptyCreate: (id: string, cwd: string) => Promise<void>
    ptyWrite: (id: string, data: string) => Promise<void>
    ptyResize: (id: string, cols: number, rows: number) => Promise<void>
    ptyKill: (id: string) => Promise<void>
    onPtyData: (id: string, callback: (data: string) => void) => () => void
  }
}
```

From src/renderer/src/assets/global.css:
```css
:root {
  --bg-main: #1a1a1a;
  --bg-panel: #242424;
  --bg-header: #2e2e2e;
  --fg-primary: #d4d4d4;
  --fg-secondary: #808080;
}
```

From src/renderer/src/App.tsx (current -- will be replaced):
```typescript
// Currently renders a single TerminalPanel in full-viewport div
// Will be replaced to render MosaicLayout instead
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create zustand panel store with unit tests</name>
  <files>src/renderer/src/store/panelStore.ts, tests/store/panelStore.test.ts, package.json</files>
  <behavior>
    - addPanel(id) adds entry with default title "Terminal" and default color "#569cd6"
    - removePanel(id) removes entry; removing non-existent id is a no-op (no crash)
    - setTitle(id, title) updates title for given id
    - setColor(id, color) updates color for given id
    - panels record is empty on initial store creation
  </behavior>
  <action>
    1. Install dependencies: `npm install react-mosaic-component zustand`
    2. After install, verify react-mosaic version is 6.x by checking `node_modules/react-mosaic-component/package.json` -- if it is v7 with n-ary tree API, the patterns in RESEARCH.md need adjustment (unlikely but verify).
    3. Create `src/renderer/src/store/panelStore.ts` with zustand `create<PanelStore>()`:
       - State: `panels: Record<string, PanelMeta>` where `PanelMeta = { title: string; color: string }`
       - Actions: `addPanel(id)`, `removePanel(id)`, `setTitle(id, title)`, `setColor(id, color)`
       - Default values on addPanel: `{ title: 'Terminal', color: '#569cd6' }`
       - Export `usePanelStore` hook AND export `PanelMeta` and `PanelStore` types
    4. Create `tests/store/panelStore.test.ts`:
       - Test all 5 behaviors listed above
       - Reset store state between tests using `usePanelStore.setState({ panels: {} })`
       - Import directly from source using `@renderer/store/panelStore` alias (vitest.config.ts has @renderer alias)
    5. Run `npm test -- tests/store/panelStore.test.ts` -- all tests must pass.

    Note: react-mosaic-component CSS import and Mosaic component usage come in Task 2.
  </action>
  <verify>
    <automated>npm test -- tests/store/panelStore.test.ts --reporter=verbose</automated>
  </verify>
  <done>panelStore.ts exports usePanelStore with all 4 actions; 5+ tests pass; react-mosaic-component and zustand are in package.json dependencies</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create MosaicLayout, PanelWindow, wire App.tsx, and fix Terminal.tsx PTY cleanup</name>
  <files>src/renderer/src/components/MosaicLayout.tsx, src/renderer/src/components/PanelWindow.tsx, src/renderer/src/App.tsx, src/renderer/src/components/Terminal.tsx, src/renderer/src/assets/global.css, tests/renderer/MosaicLayout.test.tsx</files>
  <behavior>
    - MosaicLayout renders a Mosaic component with an initial single panel (leaf = sessionId)
    - renderTile renders PanelWindow for each leaf id
    - onChange handler diffs old vs new leaves; calls window.electronAPI.ptyKill for removed leaves
    - addPanel wraps current tree root in a row split with new leaf at 50%
    - When tree is null (all panels closed), zeroStateView renders a "+ New terminal" button
    - A global "+ New terminal" button is visible and functional
    - PanelWindow wraps MosaicWindow with renderToolbar (minimal close button for now -- full header in Plan 02-02) and TerminalPanel child
    - Terminal.tsx cleanup no longer calls ptyKill (moved to MosaicLayout onChange diff)
  </behavior>
  <action>
    1. Create `tests/renderer/MosaicLayout.test.tsx` with tests for:
       - "renders initial panel" -- MosaicLayout mounts, Mosaic component appears
       - "add panel wraps root in row split" -- simulate addPanel, verify tree structure
       - "onChange updates tree state" -- trigger onChange, verify state update
       - "ptyKill on close" -- simulate removing a leaf from tree, verify ptyKill called

       CRITICAL mock setup for react-mosaic in tests:
       - Mock `react-mosaic-component` to avoid DOM complexity. Mock `Mosaic` as a component that receives `value`, `onChange`, `renderTile`, `zeroStateView` props and renders `renderTile` for leaf values. Mock `MosaicWindow` as a simple wrapper. Mock `getLeaves` as the real implementation (simple utility function -- can use the actual function or a manual mock that extracts string leaves from binary tree).
       - Mock `window.electronAPI.ptyKill` as `vi.fn()`

    2. Create `src/renderer/src/components/MosaicLayout.tsx`:
       - Import `Mosaic`, `MosaicNode`, `getLeaves` from `react-mosaic-component`
       - Import `react-mosaic-component/react-mosaic-component.css`
       - Use controlled mode: `const [tree, setTree] = useState<MosaicNode<string> | null>(initialId)` where `initialId = crypto.randomUUID()`
       - Keep a `treeRef = useRef` mirroring tree state for onChange diffing
       - `handleChange(newTree)`: diff `getLeaves(treeRef.current)` vs `getLeaves(newTree)`, call `window.electronAPI.ptyKill(id)` and `removePanel(id)` for each removed leaf, then `setTree(newTree)` and update `treeRef`
       - `handleAddPanel()`: generate `newId = crypto.randomUUID()`, call `addPanel(newId)` on store, wrap current root in `{ direction: 'row', first: current, second: newId, splitPercentage: 50 }` (handle null case: set newId as sole leaf)
       - Initialize first panel in store on mount via useEffect (not useState side-effect)
       - `renderTile={(id, path) => <PanelWindow key={id} sessionId={id} path={path} />}`
       - `zeroStateView`: dark-themed centered div with "+ New terminal" button calling handleAddPanel
       - Pass `resize={{ minimumPaneSizePercentage: 5 }}` to Mosaic (per research pitfall 5)
       - Do NOT add `className="mosaic-blueprint-theme"` (per research pitfall 4)
       - Render a global toolbar/bar at the top with "+ New terminal" button

    3. Create `src/renderer/src/components/PanelWindow.tsx`:
       - Props: `{ sessionId: string; path: MosaicBranch[] }`
       - Import `MosaicWindow` from `react-mosaic-component`
       - Wrap in `MosaicWindow<string>` with `path`, `title={sessionId}`, `createNode={() => crypto.randomUUID()}`
       - `renderToolbar={() => <div style={{ background: 'var(--bg-header)', ... }}>Panel <button>x</button></div>}` -- minimal placeholder; Plan 02-02 will replace with full PanelHeader
       - For the minimal close button: use `MosaicWindowContext` to call `mosaicActions.remove(path)`
       - Child: `<TerminalPanel sessionId={sessionId} cwd="." />`
       - Container div must have `width: 100%; height: 100%` to not break FitAddon (research pitfall 1)

    4. Update `src/renderer/src/App.tsx`:
       - Replace single TerminalPanel with `<MosaicLayout />`
       - Remove the sessionId useRef -- sessionIds are now managed inside MosaicLayout
       - Keep the full-viewport wrapper div with `background: var(--bg-main)`

    5. Update `src/renderer/src/components/Terminal.tsx`:
       - **CRITICAL CHANGE:** Remove `window.electronAPI.ptyKill(sessionId)` from the useEffect cleanup function. Keep `unsubscribe()`, `observer.disconnect()`, and `term.dispose()`. The ptyKill responsibility moves to MosaicLayout's onChange diff handler (research pitfall 2 -- double PTY kill).

    6. Add dark theme CSS overrides for react-mosaic to `src/renderer/src/assets/global.css`:
       ```css
       /* react-mosaic dark theme overrides */
       .mosaic-root { background: var(--bg-main); }
       .mosaic-tile { margin: 1px; }
       .mosaic-split { background: var(--bg-main); }
       .mosaic-split:hover { background: var(--fg-secondary); }
       .mosaic-window-toolbar { display: none; }  /* We use renderToolbar, hide default */
       .mosaic-window-body { background: var(--bg-panel); }
       ```
       Note: Audit the actual class names after install. The library may use slightly different selectors. Check `node_modules/react-mosaic-component/react-mosaic-component.css` for the canonical class names and adjust overrides accordingly.

    7. Run `npm test -- tests/renderer/MosaicLayout.test.tsx tests/store/panelStore.test.ts` -- all tests must pass.
    8. Run `npm test` -- full suite must pass (including existing 33 tests from Phase 1; Terminal.test.tsx may need adjustment if it asserts ptyKill in cleanup -- update the test to reflect the new cleanup behavior).
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose</automated>
  </verify>
  <done>MosaicLayout renders with initial panel; "+ New terminal" adds panels; drag dividers resize panels; closing a panel calls ptyKill via onChange diff; Terminal.tsx cleanup no longer double-kills PTY; all tests pass (existing + new)</done>
</task>

</tasks>

<verification>
1. `npm test` -- full suite green (existing Phase 1 tests + new panelStore + MosaicLayout tests)
2. `npm run build` -- no TypeScript or build errors
3. Visual: `npm run dev` shows tiling layout with one terminal panel on startup
4. Visual: Click "+ New terminal" -- second panel appears with live PTY
5. Visual: Drag divider between panels -- both terminals reflow text correctly
6. Visual: Close a panel -- terminal disappears, no zombie PTY processes
</verification>

<success_criteria>
- react-mosaic-component and zustand installed in package.json
- panelStore.ts with addPanel/removePanel/setTitle/setColor actions
- MosaicLayout.tsx rendering controlled Mosaic with onChange PTY lifecycle
- PanelWindow.tsx wrapping MosaicWindow + TerminalPanel
- App.tsx renders MosaicLayout instead of single TerminalPanel
- Terminal.tsx cleanup does NOT call ptyKill (moved to MosaicLayout)
- Dark theme CSS overrides prevent Blueprint style leakage
- "+ New terminal" button adds panels programmatically
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-multi-panel-layout/02-01-SUMMARY.md`
</output>
