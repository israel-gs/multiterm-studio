---
phase: 01-foundation-terminal-core
plan: 03
type: execute
wave: 3
depends_on: ['01-01', '01-02']
files_modified:
  - src/renderer/src/App.tsx
  - src/renderer/src/components/Terminal.tsx
  - tests/renderer/Terminal.test.tsx
autonomous: false
requirements:
  - TERM-03
  - TERM-04
  - TERM-05
  - TERM-06
  - TERM-07
  - TERM-08
  - TERM-09

must_haves:
  truths:
    - 'Terminal panel renders in the window and displays shell output'
    - 'User types a command and sees the correct output (real PTY, not simulated)'
    - 'Terminal renders ANSI colors correctly (256-color, 24-bit)'
    - 'Terminal handles Ctrl+C, arrow keys, and tab completion'
    - 'Clicking a URL in terminal output opens it in the system browser'
    - 'Terminal resizes correctly when the window is resized'
    - 'Terminal has 10,000+ line scrollback buffer'
    - 'User can select text to copy and paste from clipboard'
  artifacts:
    - path: 'src/renderer/src/components/Terminal.tsx'
      provides: 'xterm.js terminal component with FitAddon, WebLinksAddon, ResizeObserver'
      min_lines: 50
      contains: 'new Terminal'
    - path: 'src/renderer/src/App.tsx'
      provides: 'Root component rendering TerminalPanel'
      contains: 'TerminalPanel'
    - path: 'tests/renderer/Terminal.test.tsx'
      provides: 'Unit tests for Terminal component setup'
      contains: 'describe'
  key_links:
    - from: 'src/renderer/src/components/Terminal.tsx'
      to: 'window.electronAPI'
      via: 'ptyCreate, ptyWrite, onPtyData, ptyResize, ptyKill'
      pattern: "window\\.electronAPI\\.pty"
    - from: 'src/renderer/src/components/Terminal.tsx'
      to: '@xterm/xterm'
      via: 'new Terminal() constructor'
      pattern: 'new Terminal'
    - from: 'src/renderer/src/components/Terminal.tsx'
      to: '@xterm/addon-fit'
      via: 'fitAddon.fit() in ResizeObserver'
      pattern: "fitAddon\\.fit"
    - from: 'src/renderer/src/App.tsx'
      to: 'src/renderer/src/components/Terminal.tsx'
      via: 'import and render TerminalPanel'
      pattern: 'TerminalPanel'
---

<objective>
Create the xterm.js terminal component with FitAddon, WebLinksAddon, and ResizeObserver resize roundtrip, then wire it into App.tsx for a fully working terminal panel.

Purpose: This is the user-facing terminal. After this plan, the user can type commands, see output, resize the window, click URLs, and copy/paste -- the full Phase 1 experience.
Output: A working terminal panel connected to a real PTY session, with human verification of interactive behaviors.
</objective>

<execution_context>
@/Users/israelgs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/israelgs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-terminal-core/01-RESEARCH.md
@.planning/phases/01-foundation-terminal-core/01-01-SUMMARY.md
@.planning/phases/01-foundation-terminal-core/01-02-SUMMARY.md

<interfaces>
<!-- From Plan 01: Window type augmentation -->

From src/renderer/src/env.d.ts:

```typescript
interface Window {
  electronAPI: {
    ptyCreate: (id: string, cwd: string) => Promise<void>
    ptyWrite: (id: string, data: string) => Promise<void>
    ptyResize: (id: string, cols: number, rows: number) => Promise<void>
    ptyKill: (id: string) => Promise<void>
    onPtyData: (id: string, callback: (data: string) => void) => () => void
  }
}
```

<!-- From Plan 01: CSS custom properties -->

From src/renderer/src/assets/global.css:

```css
:root {
  --bg-main: #1a1a1a;
  --bg-panel: #242424;
  --bg-header: #2e2e2e;
  --fg-primary: #d4d4d4;
  --fg-secondary: #808080;
}
```

<!-- From Plan 01: Dark theme for xterm.js -->

xterm.js ITheme (from RESEARCH):

```typescript
const darkTheme = {
  background: '#1a1a1a',
  foreground: '#d4d4d4',
  cursor: '#d4d4d4',
  selectionBackground: '#264f78'
  // ... full 16-color palette in RESEARCH
}
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Terminal component with xterm.js, addons, and resize roundtrip</name>
  <files>src/renderer/src/components/Terminal.tsx, src/renderer/src/App.tsx, tests/renderer/Terminal.test.tsx</files>
  <behavior>
    - Terminal component creates xterm.js Terminal with scrollback:10000, fontSize:14, cursorBlink:true
    - Terminal component loads FitAddon and WebLinksAddon
    - Terminal component imports @xterm/xterm/css/xterm.css (CRITICAL: blank without it)
    - Terminal component calls term.open(container) then fitAddon.fit() (order matters)
    - Terminal component calls window.electronAPI.ptyCreate on mount
    - Terminal component wires term.onData to window.electronAPI.ptyWrite
    - Terminal component wires window.electronAPI.onPtyData to term.write
    - Terminal component creates ResizeObserver that calls fitAddon.fit() then ptyResize
    - Terminal cleanup calls unsubscribe(), observer.disconnect(), ptyKill(), term.dispose()
    - App.tsx renders TerminalPanel with a generated sessionId and cwd of process.cwd()
  </behavior>
  <action>
  First, create `tests/renderer/Terminal.test.tsx`:

**Mocking strategy for renderer tests:**

- Mock `@xterm/xterm` with vi.mock: Terminal constructor returns mock with `loadAddon`, `open`, `onData`, `write`, `dispose`, `cols`, `rows` properties
- Mock `@xterm/addon-fit` with vi.mock: FitAddon constructor returns mock with `fit` method
- Mock `@xterm/addon-web-links` with vi.mock: WebLinksAddon constructor
- Mock `window.electronAPI` on the global window object with vi.stubGlobal or Object.defineProperty

**Test cases:**

- Terminal is created with scrollback:10000 option
- FitAddon and WebLinksAddon are loaded via term.loadAddon
- term.open is called with the container ref
- fitAddon.fit() is called after term.open
- window.electronAPI.ptyCreate is called with sessionId and cwd
- term.onData callback calls window.electronAPI.ptyWrite
- window.electronAPI.onPtyData is called and its callback calls term.write
- Cleanup function calls ptyKill, term.dispose

Then implement `src/renderer/src/components/Terminal.tsx` per RESEARCH Pattern 5:

```tsx
import { useEffect, useRef } from 'react'
import { Terminal } from '@xterm/xterm'
import { FitAddon } from '@xterm/addon-fit'
import { WebLinksAddon } from '@xterm/addon-web-links'
import '@xterm/xterm/css/xterm.css'
```

Props: `{ sessionId: string; cwd: string }`

useEffect (deps: [sessionId, cwd]):

1. Guard: if (!containerRef.current) return
2. Create Terminal with full config: scrollback 10000, fontSize 14, fontFamily `'JetBrains Mono', 'Fira Code', Menlo, monospace`, cursorBlink true, cursorStyle 'block', theme with all 16 colors from RESEARCH, convertEol false
3. Create FitAddon and WebLinksAddon, loadAddon both
4. term.open(containerRef.current) -- MUST be before fitAddon.fit()
5. fitAddon.fit()
6. window.electronAPI.ptyCreate(sessionId, cwd)
7. term.onData((data) => window.electronAPI.ptyWrite(sessionId, data))
8. const unsubscribe = window.electronAPI.onPtyData(sessionId, (data) => term.write(data))
9. ResizeObserver on containerRef.current: fitAddon.fit(), then ptyResize(sessionId, term.cols, term.rows)
10. Return cleanup: unsubscribe(), observer.disconnect(), ptyKill(sessionId), term.dispose()

Container div: `style={{ width: '100%', height: '100%' }}`

Then update `src/renderer/src/App.tsx`:

- Import TerminalPanel from './components/Terminal'
- Generate a sessionId with `crypto.randomUUID()` (or a useRef to keep it stable)
- For cwd, use `'.'` as the default. In ptyManager.ts (from Plan 02), resolve `'.'` to `process.cwd()` using `import { resolve } from 'path'` and `cwd: resolve(cwd)`. This keeps the renderer simple and the main process authoritative over paths.
- Render: full-viewport div with TerminalPanel inside: `<div style={{ width: '100vw', height: '100vh', background: 'var(--bg-main)' }}><TerminalPanel sessionId={sessionId} cwd="." /></div>`
  </action>
  <verify>
  <automated>cd /Users/israelgs/Documents/work/personal/multiterm-studio && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>Terminal.tsx component renders xterm.js with FitAddon, WebLinksAddon, scrollback 10000, dark theme. Connected to PTY via IPC. ResizeObserver triggers resize roundtrip. All unit tests pass.</done>
  </task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete terminal experience</name>
  <files>n/a</files>
  <action>
  Human verification of the complete Phase 1 terminal experience. All interactive behaviors (ANSI colors, keyboard input, URL clicking, copy/paste, resize, scrollback) require visual/manual confirmation that cannot be automated.
  </action>
  <verify>User confirms all 12 verification steps pass</verify>
  <done>All Phase 1 terminal behaviors confirmed working by human tester</done>
  <what-built>Complete Phase 1 terminal: Electron app with real PTY shell session, xterm.js rendering, ANSI colors, resize support, clickable URLs, keyboard passthrough, and copy/paste</what-built>
  <how-to-verify>
    1. Run `cd /Users/israelgs/Documents/work/personal/multiterm-studio && npm run dev`
    2. An Electron window should appear with a dark background (#1a1a1a) and a terminal prompt
    3. Type `pwd` and press Enter -- should show the current working directory (TERM-02)
    4. Type `ls --color` and press Enter -- should show colorized file listing (TERM-06)
    5. Type `echo "Hello world"` -- should render emoji and Unicode (TERM-07)
    6. Run `cat` (or any long-running process) and press Ctrl+C -- should interrupt (TERM-08)
    7. Use arrow keys to navigate command history (TERM-08)
    8. Type `echo "https://github.com"` -- the URL should be clickable and open in browser (TERM-03, WebLinksAddon)
    9. Select some terminal text with mouse, then Cmd+C / Cmd+V to copy/paste (TERM-09)
    10. Resize the Electron window -- terminal should re-render correctly filling the space (TERM-04)
    11. Scroll up through output history to verify scrollback works (TERM-05)
    12. Run `printf '\e[38;5;196mRed\e[0m \e[38;2;0;255;0mGreen\e[0m'` -- should show colored text (TERM-06)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe which checks failed and what you observed</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` all tests green (unit tests for Terminal component + all prior tests)
- `npm run dev` opens Electron app with working terminal (human-verified)
- Terminal renders real shell output (not simulated)
- Resize, colors, Unicode, keyboard, clipboard all functional
</verification>

<success_criteria>

- xterm.js terminal renders in the Electron window with dark theme
- FitAddon and WebLinksAddon are loaded
- Terminal connects to real PTY session (user can run commands)
- Shell starts with correct cwd
- ResizeObserver triggers fit + IPC resize when window changes size
- Scrollback buffer holds 10,000+ lines
- ANSI colors (256 + 24-bit) render correctly
- Ctrl+C, arrow keys, tab completion work
- URLs in terminal output are clickable
- Copy and paste work
- All automated tests pass
- Human verification checkpoint approved
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-terminal-core/01-03-SUMMARY.md`
</output>
