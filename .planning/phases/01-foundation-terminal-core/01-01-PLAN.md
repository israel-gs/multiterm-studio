---
phase: 01-foundation-terminal-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - electron.vite.config.ts
  - tsconfig.json
  - tsconfig.node.json
  - tsconfig.web.json
  - src/main/index.ts
  - src/preload/index.ts
  - src/preload/index.d.ts
  - src/renderer/index.html
  - src/renderer/src/main.tsx
  - src/renderer/src/App.tsx
  - src/renderer/src/env.d.ts
  - src/renderer/src/assets/global.css
  - vitest.config.ts
  - tests/main/window.test.ts
  - tests/main/ptyManager.test.ts
  - tests/renderer/theme.test.ts
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-04
  - INFRA-05

must_haves:
  truths:
    - "Electron app starts with `npm run dev` showing a dark-themed window"
    - "BrowserWindow uses contextIsolation:true and nodeIntegration:false"
    - "Preload script exposes all PTY IPC methods via contextBridge"
    - "Preload push listener (onPtyData) returns an unsubscribe closure"
    - "node-pty is externalized from Vite bundle"
    - "postinstall script rebuilds native modules for Electron ABI"
  artifacts:
    - path: "src/main/index.ts"
      provides: "BrowserWindow with secure webPreferences"
      contains: "contextIsolation: true"
    - path: "src/preload/index.ts"
      provides: "contextBridge API with all IPC channels"
      exports: ["electronAPI"]
    - path: "src/renderer/src/env.d.ts"
      provides: "Window type augmentation for electronAPI"
      contains: "interface Window"
    - path: "electron.vite.config.ts"
      provides: "Vite config with node-pty externalized"
      contains: "node-pty"
    - path: "package.json"
      provides: "Dependencies, scripts including postinstall"
      contains: "postinstall"
    - path: "vitest.config.ts"
      provides: "Test configuration for main and renderer tests"
    - path: "tests/main/window.test.ts"
      provides: "Test stubs for INFRA-01"
    - path: "tests/main/ptyManager.test.ts"
      provides: "Test stubs for INFRA-02, TERM-01, TERM-02"
    - path: "tests/renderer/theme.test.ts"
      provides: "Test stubs for INFRA-05"
  key_links:
    - from: "src/main/index.ts"
      to: "src/preload/index.ts"
      via: "webPreferences.preload path"
      pattern: "preload.*join.*preload"
    - from: "electron.vite.config.ts"
      to: "node-pty"
      via: "rollupOptions.external"
      pattern: "external.*node-pty"
---

<objective>
Scaffold the complete Electron + electron-vite project with secure IPC infrastructure, dark theme, postinstall native rebuild, and Wave 0 test scaffolds.

Purpose: Establishes the foundation that all subsequent plans build on -- BrowserWindow, preload bridge, project structure, build tooling, and test infrastructure.
Output: A running Electron app (dark window, no terminal yet), complete IPC bridge ready for PTY handlers, and test scaffolds covering INFRA-01/02/04/05.
</objective>

<execution_context>
@/Users/israelgs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/israelgs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-terminal-core/01-RESEARCH.md
@.planning/phases/01-foundation-terminal-core/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Electron project with electron-vite and configure build</name>
  <files>package.json, electron.vite.config.ts, tsconfig.json, tsconfig.node.json, tsconfig.web.json, src/main/index.ts, src/preload/index.ts, src/preload/index.d.ts, src/renderer/index.html, src/renderer/src/main.tsx, src/renderer/src/App.tsx, src/renderer/src/env.d.ts, src/renderer/src/assets/global.css</files>
  <action>
  Scaffold with `npm create @quick-start/electron@latest multiterm-studio -- --template react-ts` (run from parent directory, then move contents into the current project root since .planning already exists -- or scaffold into a temp directory and copy).

  After scaffold, make these modifications:

  1. **package.json**: Add production dependencies `node-pty`, `@xterm/xterm`, `@xterm/addon-fit`, `@xterm/addon-web-links`. Add `"postinstall": "electron-builder install-app-deps"` to scripts. Ensure `node-pty` is in `dependencies` (NOT devDependencies). Run `npm install` after edits.

  2. **electron.vite.config.ts**: Replace content with the config from RESEARCH Pattern 1. Externalize `node-pty` in both `main` and `preload` via `rollupOptions.external`. Use `@vitejs/plugin-react` for renderer. Do NOT use the deprecated `externalizeDepsPlugin`.

  3. **src/main/index.ts**: Create BrowserWindow with:
     - `width: 1200, height: 800`
     - `backgroundColor: '#1a1a1a'`
     - `webPreferences: { preload: join(__dirname, '../preload/index.js'), contextIsolation: true, nodeIntegration: false, sandbox: false }`
     - Load the renderer URL (electron-vite provides `process.env.ELECTRON_RENDERER_URL` in dev, file path in production -- follow the scaffold pattern for this)
     - Import and call `registerPtyHandlers(win.webContents)` from `./ptyManager` (the function will be a stub that does nothing until Plan 02 implements it)
     - Create a stub `src/main/ptyManager.ts` that exports `function registerPtyHandlers(webContents: WebContents): void {}` as an empty function

  4. **src/preload/index.ts**: Implement the full contextBridge API per RESEARCH Pattern 4:
     - `ptyCreate(id, cwd)` -> `ipcRenderer.invoke('pty:create', id, cwd)`
     - `ptyWrite(id, data)` -> `ipcRenderer.invoke('pty:write', id, data)`
     - `ptyResize(id, cols, rows)` -> `ipcRenderer.invoke('pty:resize', id, cols, rows)`
     - `ptyKill(id)` -> `ipcRenderer.invoke('pty:kill', id)`
     - `onPtyData(id, callback)` -> MUST use the unsubscribe closure pattern: create `listener` wrapper inside the function, register with `ipcRenderer.on`, return `() => ipcRenderer.removeListener(channel, listener)`. This is CRITICAL per the contextBridge bug (electron#33328).

  5. **src/renderer/src/env.d.ts**: Add Window interface augmentation for `electronAPI` with full type signatures (see RESEARCH Pattern 4 TypeScript section). Keep any existing electron-vite env types.

  6. **src/renderer/src/App.tsx**: Minimal placeholder component that renders a dark div with text "Multiterm Studio" centered. This is temporary -- Plan 03 replaces it.

  7. **src/renderer/src/assets/global.css**: Set CSS custom properties on `:root`:
     - `--bg-main: #1a1a1a`
     - `--bg-panel: #242424`
     - `--bg-header: #2e2e2e`
     - `--fg-primary: #d4d4d4`
     - `--fg-secondary: #808080`
     Body styles: `margin: 0; padding: 0; background: var(--bg-main); color: var(--fg-primary); font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif; overflow: hidden; height: 100vh; width: 100vw;`
     Import this CSS in `src/renderer/src/main.tsx`.

  Verify the scaffold runs: `npm run dev` should open a dark Electron window. Kill it after visual confirmation.
  </action>
  <verify>
    <automated>cd /Users/israelgs/Documents/work/personal/multiterm-studio && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Electron app scaffolded with electron-vite react-ts template. BrowserWindow has contextIsolation:true, nodeIntegration:false. Preload exposes all 5 IPC methods with unsubscribe pattern. node-pty externalized in vite config. postinstall rebuilds native modules. Dark theme CSS variables set. `npm run build` succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Create Wave 0 test scaffolds and vitest configuration</name>
  <files>vitest.config.ts, tests/main/window.test.ts, tests/main/ptyManager.test.ts, tests/renderer/theme.test.ts</files>
  <action>
  Install test dependencies: `npm install --save-dev vitest @testing-library/react @testing-library/jest-dom jsdom` (if not already present from scaffold).

  1. **vitest.config.ts**: Create at project root. Use `defineConfig` from vitest/config. Set:
     - `test.globals: true`
     - `test.environment: 'jsdom'` (default for renderer tests)
     - `test.include: ['tests/**/*.test.{ts,tsx}']`
     - `test.alias` mapping `@renderer` to `src/renderer/src` if needed by the scaffold's path aliases

  2. **tests/main/window.test.ts** (INFRA-01):
     - Test: "BrowserWindow config uses contextIsolation:true and nodeIntegration:false"
     - Approach: Import `src/main/index.ts` and verify the webPreferences object. Since this requires mocking Electron, use a simpler approach: read the source file as text and assert it contains `contextIsolation: true` and `nodeIntegration: false`. This is a static verification test.
     - Also assert `sandbox: false` is present (required for preload Node APIs).

  3. **tests/main/ptyManager.test.ts** (INFRA-02, TERM-01, TERM-02):
     - These are STUB tests that will fail until Plan 02 implements ptyManager.
     - Test: "ptyManager registers pty:create handler" -> `test.todo('pty:create handler spawns a PTY session')`
     - Test: "ptyManager registers pty:write handler" -> `test.todo('pty:write handler writes to PTY')`
     - Test: "ptyManager registers pty:resize handler" -> `test.todo('pty:resize handler resizes PTY')`
     - Test: "ptyManager registers pty:kill handler" -> `test.todo('pty:kill handler kills PTY and removes session')`
     - Test: "PTY spawns with user's shell" -> `test.todo('spawns with process.env.SHELL')`
     - Test: "PTY spawns with provided cwd" -> `test.todo('spawns with cwd from IPC argument')`
     - Mark all as `test.todo()` so they show as pending, not failing.

  4. **tests/renderer/theme.test.ts** (INFRA-05):
     - Test: "CSS custom properties define correct dark theme colors"
     - Approach: Import the CSS file content (read as string) and assert it contains `--bg-main: #1a1a1a`, `--bg-panel: #242424`, `--bg-header: #2e2e2e`.
     - Alternative: Use jsdom to load the stylesheet and check `getComputedStyle(document.documentElement).getPropertyValue('--bg-main')`.

  Add `"test": "vitest run --reporter=verbose"` to package.json scripts if not present.
  </action>
  <verify>
    <automated>cd /Users/israelgs/Documents/work/personal/multiterm-studio && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>Vitest configured. window.test.ts passes (verifies contextIsolation/nodeIntegration in source). ptyManager.test.ts has 6 todo stubs (pending, not failing). theme.test.ts passes (verifies CSS custom property values). `npx vitest run` shows green for implemented tests, pending for todos.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors (Vite builds main, preload, renderer)
- `npx vitest run --reporter=verbose` shows green tests for INFRA-01 and INFRA-05, pending todos for PTY tests
- `grep -c "contextIsolation: true" src/main/index.ts` returns 1
- `grep -c "node-pty" electron.vite.config.ts` returns at least 1 (externalized)
- `grep -c "postinstall" package.json` returns 1
- `grep -c "removeListener" src/preload/index.ts` returns at least 1 (unsubscribe pattern)
</verification>

<success_criteria>
- Electron app builds successfully with electron-vite
- BrowserWindow configured with contextIsolation:true, nodeIntegration:false, sandbox:false
- Preload exposes ptyCreate, ptyWrite, ptyResize, ptyKill, onPtyData via contextBridge
- onPtyData uses the unsubscribe closure pattern (not direct removeListener)
- node-pty externalized in rollupOptions for both main and preload
- postinstall script runs electron-builder install-app-deps
- Dark theme CSS variables match spec (#1a1a1a, #242424, #2e2e2e)
- Vitest runs with green results for static tests, pending for PTY stubs
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-terminal-core/01-01-SUMMARY.md`
</output>
